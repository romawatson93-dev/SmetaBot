# 📋 SmetaBot — Техническое задание проекта

## 📑 Содержание

1. [Общее описание проекта](#1-общее-описание-проекта)
2. [Цели и задачи системы](#2-цели-и-задачи-системы)
3. [Роли пользователей](#3-роли-пользователей)
4. [Архитектура системы](#4-архитектура-системы)
5. [Функциональные требования](#5-функциональные-требования)
6. [Технические требования](#6-технические-требования)
7. [API спецификации](#7-api-спецификации)
8. [Процессы и сценарии использования](#8-процессы-и-сценарии-использования)
9. [База данных](#9-база-данных)
10. [Безопасность](#10-безопасность)
11. [Инфраструктура и развёртывание](#11-инфраструктура-и-развёртывание)
12. [Мониторинг и метрики](#12-мониторинг-и-метрики)
13. [Ограничения и известные проблемы](#13-ограничения-и-известные-проблемы)
14. [Планы развития](#14-планы-развития)

---

## 1. Общее описание проекта

### 1.1. Назначение

**SmetaBot** — это SaaS-сервис для автоматизации защищённой выдачи смет и проектной документации подрядчиков клиентам через Telegram. Система обеспечивает:

- Автоматическое создание приватных Telegram-каналов с защитой контента
- Конвертацию документов (PDF, DOC, XLS) в изображения высокого качества (PNG 300 DPI)
- Наложение водяных знаков на все публикуемые материалы
- Управление одноразовыми ссылками-приглашениями
- Отслеживание статистики просмотров и активности клиентов
- Панель управления для подрядчиков и администраторов

### 1.2. Основные возможности

- **Защищённая публикация**: Все документы публикуются как `document` с флагом `protect_content=True`, запрещён форвардинг
- **Конвертация документов**: Поддержка PDF, DOC/DOCX, XLS/XLSX/ODS с автоматическим рендерингом в PNG 300 DPI
- **Водяные знаки**: Настраиваемые тайловые водяные знаки на всех изображениях
- **Одноразовые инвайты**: Ссылки-приглашения с ограничением `member_limit=1`
- **Статистика**: Отслеживание просмотров, клиентов, публикаций
- **Многопользовательская архитектура**: Отдельные MTProto-сессии для каждого подрядчика

---

## 2. Цели и задачи системы

### 2.1. Бизнес-цели

1. **Защита интеллектуальной собственности**: Предотвращение несанкционированного копирования и распространения смет
2. **Автоматизация процессов**: Снижение ручного труда при выдаче документов клиентам
3. **Контроль доступа**: Управление доступом клиентов к проектной документации
4. **Аналитика**: Сбор данных о просмотрах и активности для оптимизации бизнес-процессов

### 2.2. Технические цели

1. **Масштабируемость**: Поддержка 1-5 тысяч подрядчиков
2. **Надёжность**: Обработка ошибок Telegram API, retry-механизмы, мониторинг
3. **Безопасность**: Шифрование сессий, защита контента, аудит действий
4. **Производительность**: Асинхронная обработка, очереди задач, оптимизация рендеринга

---

## 3. Роли пользователей

### 3.1. Владелец (Owner)

**Права и возможности:**
- Управление подрядчиками (блокировка, активация)
- Управление подписками и тарифами
- Просмотр глобальной статистики
- Администрирование системы
- Управление квотами и лимитами

**Доступ:**
- Админ-бот в Telegram
- Backend API с правами администратора
- Панель управления (планируется)

### 3.2. Подрядчик (Contractor)

**Права и возможности:**
- Создание приватных каналов для проектов
- Загрузка и публикация документов (PDF, DOC, XLS, PNG)
- Создание и управление ссылками-приглашениями
- Просмотр статистики по своим каналам
- Управление профилем (аватарка для каналов)
- Просмотр лимитов подписки

**Ограничения:**
- Зависят от тарифного плана (FREE/PRO/BUSINESS)
- Лимиты на количество каналов
- Лимиты на количество публикаций

### 3.3. Клиент (Client)

**Права и возможности:**
- Доступ к приватному каналу по одноразовой ссылке
- Просмотр опубликованных документов
- Просмотр смет и проектной документации

**Ограничения:**
- Нет возможности форвардить контент (защита включена)
- Нет возможности скачивать оригинальные файлы (только PNG с водяным знаком)
- Доступ только к назначенным каналам

---

## 4. Архитектура системы

### 4.1. Общая архитектура

Система построена на микросервисной архитектуре с разделением ответственности:

```
┌─────────────────┐
│  Telegram Bot   │  ← Интерфейс для подрядчиков
│   (aiogram)     │
└────────┬────────┘
         │
    ┌────┴────┐
    │         │
┌───▼───┐ ┌──▼──────┐
│Redis  │ │PostgreSQL│
│(Queue)│ │  (DB)   │
└───┬───┘ └─────────┘
    │
┌───▼──────────────┐
│  Celery Workers  │  ← Фоновая обработка
│  (PDF/Office/    │
│   Publish)       │
└──────────────────┘

┌─────────────────┐
│  Userbot API    │  ← MTProto операции
│  (FastAPI +     │
│   Telethon)     │
└─────────────────┘

┌─────────────────┐
│  Backend API    │  ← REST API (планируется)
│  (FastAPI)      │
└─────────────────┘
```

### 4.2. Компоненты системы

#### 4.2.1. Telegram Bot (`bot/`)

**Технологии:**
- Python 3.11+
- aiogram 3.x
- PostgreSQL (asyncpg)
- Redis (aioredis)

**Обязанности:**
- Обработка команд и callback-запросов от пользователей
- Управление FSM (Finite State Machine) для многошаговых процессов
- Интеграция с Userbot API для операций с каналами
- Постановка задач в очередь Celery
- Взаимодействие с базой данных для хранения метаданных
- Генерация превью документов через Celery
- Управление инвайтами и join-requests

**Основные обработчики:**
- `handlers/start.py` — стартовое меню и онбординг
- `handlers/menu.py` — главное меню подрядчика
- `handlers/channel_wizard.py` — мастер создания канала
- `handlers/render_pdf.py` — загрузка и обработка документов
- `handlers/my_channels.py` — управление каналами и статистика
- `handlers/profile.py` — личный кабинет
- `handlers/webapp_gate.py` — интеграция с WebApp для авторизации
- `handlers/invite_manage.py` — управление инвайтами

**Сервисы:**
- `services/channels.py` — работа с каналами и публикациями
- `services/contractors.py` — управление подрядчиками
- `services/billing.py` — проверка лимитов и подписок
- `services/events.py` — логирование событий
- `services/invites.py` — управление ссылками-приглашениями

#### 4.2.2. Userbot API (`userbot/`)

**Технологии:**
- Python 3.11+
- FastAPI
- Telethon (MTProto)
- Fernet (шифрование сессий)

**Обязанности:**
- Создание и управление Telegram-каналами от имени подрядчиков
- Авторизация через телефонный номер (с поддержкой 2FA)
- WebApp-интерфейс для входа
- Управление правами администраторов в каналах
- Получение статистики просмотров сообщений
- Установка фото каналов

**Эндпоинты:**

**Сессии:**
- `GET /session/status` — проверка наличия и статуса сессии
  - Параметры: `contractor_id` (str), `verify` (bool, опционально)
  - Ответ: `{has_session: bool, authorized: bool}`

**Авторизация:**
- `POST /login/phone/start` — начало телефонного логина
  - Тело: `{contractor_id: str, phone: str}`
  - Ответ: `{token: str}`
- `POST /login/phone/confirm` — подтверждение кода
  - Тело: `{token: str, code: str}`
  - Ответ: `{status: "ready" | "2fa_required", me?: {...}}`
- `POST /login/phone/2fa` — ввод пароля 2FA
  - Тело: `{token: str, password: str}`
  - Ответ: `{status: "ready", me?: {...}}`

**WebApp:**
- `GET /webapp/login` — страница входа через WebApp
- `GET /webapp/login2` — альтернативная страница входа

**Каналы:**
- `POST /rooms/create` — создание приватного канала
  - Тело: `{contractor_id: str, title: str, about?: str}`
  - Ответ: `{channel_id: int}`
  - Действия: создаёт канал, включает `ToggleNoForwardsRequest`
- `POST /rooms/add_bot_admin` — назначение бота администратором
  - Тело: `{contractor_id: str, channel_id: int, bot_username: str}`
  - Права: постинг, инвайты, изменение информации
- `POST /rooms/set_photo` — установка фото канала
  - Тело: `{contractor_id: str, channel_id: int, photo_b64: str}`
- `POST /rooms/get_views` — получение просмотров сообщений
  - Тело: `{contractor_id: str, channel_id: int, message_ids?: List[int], limit?: int}`
  - Ответ: `{ok: bool, views: Dict[int, int], error?: str}`
- `POST /rooms/get_admins` — список администраторов канала
  - Тело: `{contractor_id: str, channel_id: int, limit?: int}`
  - Ответ: `{ok: bool, admins: List[AdminInfo], error?: str}`

**Особенности:**
- Каждому подрядчику соответствует отдельная MTProto-сессия
- Сессии шифруются Fernet перед сохранением на диск
- Автоматическая обработка FloodWait через `with_floodwait`
- Закрытие клиентов после операций для экономии ресурсов

#### 4.2.3. Celery Worker (`worker/`)

**Технологии:**
- Python 3.11+
- Celery
- Redis (брокер и результат)
- PyMuPDF (fitz) — конвертация PDF
- LibreOffice — конвертация DOC/XLS
- Pillow — обработка изображений
- Ghostscript — обработка PDF (опционально)

**Очереди:**
- `pdf` — обработка PDF-документов
- `office` — обработка DOC/DOCX/XLS/XLSX
- `publish` — публикация в каналы
- `preview` — генерация превью

**Задачи:**

**Рендеринг:**
- `tasks.render.process_and_publish_pdf` — PDF → PNG 300 DPI + публикация
- `tasks.render.process_and_publish_doc` — DOC/DOCX → PNG 300 DPI + публикация
- `tasks.render.process_and_publish_excel` — XLS/XLSX → PNG 300 DPI + публикация
- `tasks.render.process_and_publish_png` — PNG → нормализация DPI + водяной знак + публикация

**Превью:**
- `tasks.preview.generate_preview_task` — генерация превью для выбора страниц

**Публикация:**
- `tasks.publish.send_document` — отправка документа в канал через Bot API
  - Параметры: `chat_id`, `document_bytes`, `filename`, `caption`
  - Флаги: `protect_content=True`
  - Retry при ошибках 403/429/400

**Статистика:**
- `tasks.stats.update_views_daily` — обновление ежедневной статистики просмотров
- `tasks.stats.update_channel_stats` — агрегация статистики по каналам
- `tasks.stats.refresh_views_periodic` — периодическое обновление просмотров
- `tasks.stats.apply_queued_gifts` — применение подарков из очереди

**Процесс обработки документа:**

1. **Загрузка**: Бот сохраняет файл в Redis с ключом `pdf:<uuid>`, `doc:<uuid>`, `xls:<uuid>` или `png:<uuid>`
2. **Превью**: Для PDF/DOC/XLS генерируется превью через `generate_preview_task`
3. **Выбор страниц**: Пользователь выбирает страницы/листы для публикации
4. **Рендеринг**: Задача ставится в очередь (`pdf`, `office` или `publish`)
5. **Конвертация**: 
   - PDF: PyMuPDF → PNG 300 DPI
   - DOC/DOCX: LibreOffice → PDF → PyMuPDF → PNG 300 DPI
   - XLS/XLSX: LibreOffice → PDF → PyMuPDF → PNG 300 DPI
6. **Водяной знак**: Наложение тайлового водяного знака (если указан текст)
7. **Публикация**: Отправка через Bot API с `protect_content=True`
8. **Запись в БД**: Метаданные сохраняются в `core.publications`

#### 4.2.4. Backend API (`backend/`)

**Технологии:**
- Python 3.11+
- FastAPI
- PostgreSQL (asyncpg)

**Текущее состояние:**
- Минимальная реализация
- `GET /health` — health-check
- WebApp демо-страницы

**Планируется:**
- REST API для CRUD операций с проектами, каналами, документами, инвайтами
- Интеграция с системой биллинга
- API для мобильных приложений
- Webhook-эндпоинты для внешних интеграций

#### 4.2.5. Общие библиотеки (`common/`)

**`common/preview.py`:**
- Генерация превью документов
- Поддержка PDF (PyMuPDF), DOC/DOCX (LibreOffice), XLS/XLSX (LibreOffice + openpyxl)
- Поиск таблиц в Excel
- Конвертация в JPEG для превью

**`common/watermark.py`:**
- Загрузка настроек водяного знака из переменных окружения
- Параметры: opacity, step, angle, color, font, scale
- Дефолтные значения и валидация

---

## 5. Функциональные требования

### 5.1. Онбординг подрядчика

**FR-1.1. Стартовое меню**
- При команде `/start` бот показывает главное меню
- Если сессия не авторизована — предлагается подключить аккаунт через WebApp
- В dev-средах доступен прямой телефонный логин (`/phone`)

**FR-1.2. Авторизация через WebApp**
- Пользователь открывает WebApp из бота
- WebApp проверяет существующую сессию
- Если сессии нет — запускается телефонный flow:
  1. Ввод номера телефона
  2. Получение кода в Telegram/SMS
  3. Ввод кода
  4. При необходимости — ввод пароля 2FA
- После успешной авторизации WebApp отправляет `session_ready` в бота
- Бот проверяет сессию через `/session/status?verify=true`
- Разблокируется основное меню

**FR-1.3. Авторизация через телефон (dev)**
- Команда `/phone` или callback `conn_phone`
- FSM: `waiting_phone` → `waiting_code` → `waiting_password` (если нужно)
- Взаимодействие с `/login/phone/*` эндпоинтами
- После успеха — обновление меню

**FR-1.4. Проверка сессии**
- Кнопка "🔎 Проверить подключение"
- Вызов `/session/status?verify=true`
- Отображение статуса авторизации

### 5.2. Создание защищённого канала

**FR-2.1. Мастер создания канала**
- Раздел "🆕 Новый канал" или команда `/new`
- FSM: `waiting_title` → `waiting_avatar`
- Ввод названия канала (до 64 символов)
- Загрузка аватарки (опционально):
  - Кастомное фото
  - Стандартная аватарка из профиля
  - Пропустить

**FR-2.2. Создание канала через Userbot**
- Проверка наличия авторизованной сессии
- Вызов `/rooms/create` с названием
- Userbot создаёт приватный канал
- Включается `ToggleNoForwardsRequest` (запрет форвардинга)
- Канал создаётся с `protected_content` (защита контента)

**FR-2.3. Назначение бота администратором**
- Вызов `/rooms/add_bot_admin`
- Бот получает права:
  - `post_messages=True` — публикация сообщений
  - `invite_users=True` — приглашение пользователей
  - `change_info=True` — изменение информации
  - Остальные права — `False`

**FR-2.4. Установка фото канала**
- Если загружена аватарка — установка через Bot API или `/rooms/set_photo`
- Конвертация в JPEG при необходимости
- Обработка ошибок

**FR-2.5. Сохранение метаданных**
- Запись в `core.channels`:
  - `contractor_id` (внутренний ID из `core.contractors`)
  - `tg_chat_id` (Telegram chat_id канала)
  - `title`, `username`, `created_at`
- Проверка лимитов подписки через `billing.can_create_channel`
- Инкремент счётчика `billing.usage_counters.channels_created_total`

**FR-2.6. Создание одноразовой ссылки**
- `Bot.create_chat_invite_link` с параметрами:
  - `creates_join_request=True` — требуется одобрение
  - `member_limit=1` — одноразовая ссылка (опционально, может быть 0 для неограниченных)
- Сохранение в `core.invites`:
  - `channel_id`, `token` (ссылка), `max_uses=1`, `used_count=0`
- Отображение ссылки пользователю

### 5.3. Загрузка и публикация документов

**FR-3.1. Выбор формата**
- Меню "Рендер файлов":
  - "📄 PDF → PNG"
  - "📊 Excel → PNG"
  - "📝 Word → PNG"
  - "🖼️ PNG в канал"

**FR-3.2. Загрузка файла**
- Пользователь отправляет файл в чат
- Бот сохраняет в Redis:
  - Ключ: `pdf:<uuid>`, `doc:<uuid>`, `xls:<uuid>` или `png:<uuid>`
  - TTL: `SOURCE_BLOB_TTL` (по умолчанию 24 часа)
- Валидация формата файла

**FR-3.3. Генерация превью (для PDF/DOC/XLS)**
- Задача `generate_preview_task` ставится в очередь `preview`
- Генерация превью через `common/preview.py`:
  - PDF: PyMuPDF → PNG → JPEG (thumbnail)
  - DOC/DOCX: LibreOffice → PDF → PyMuPDF → PNG → JPEG
  - XLS/XLSX: LibreOffice → PDF → PyMuPDF → PNG → JPEG (или openpyxl для поиска таблиц)
- Превью сохраняется в Redis: `preview:<uuid>` с TTL `PREVIEW_BLOB_TTL`
- Отображение превью пользователю для выбора страниц

**FR-3.4. Выбор страниц и настройка водяного знака**
- Для многостраничных документов — выбор страниц/листов
- Ввод текста водяного знака (по умолчанию — username подрядчика)
- Подтверждение публикации

**FR-3.5. Рендеринг и публикация**
- Задача ставится в очередь:
  - PDF → `pdf`
  - DOC/DOCX → `office`
  - XLS/XLSX → `office`
  - PNG → `publish`
- Процесс:
  1. Извлечение файла из Redis (однократное чтение с удалением)
  2. Конвертация в PNG 300 DPI
  3. Наложение водяного знака (если указан текст)
  4. Отправка через `send_document` с `protect_content=True`
  5. Запись метаданных в `core.publications`

**FR-3.6. Защита от одновременных рендеров**
- Per-user `asyncio.Lock` для предотвращения нескольких рендеров одновременно
- Блокировка интерфейса во время обработки

### 5.4. Управление каналами

**FR-4.1. Список каналов**
- Раздел "📢 Мои каналы"
- Отображение:
  - Суммарная статистика: каналы, файлы, просмотры
  - Список последних проектов
  - Поиск по названию канала
- Карточка канала:
  - Название, дата создания
  - Статистика: файлы, просмотры, клиенты
  - Список администраторов (через `/rooms/get_admins`)

**FR-4.2. Статистика канала**
- Агрегация из `core.publications` и `analytics.channel_stats`
- Просмотры из `analytics.views_daily`
- Клиенты из `core.clients`
- Активные инвайты из `core.invites`

**FR-4.3. Синхронизация каналов**
- Функция `sync_channels_from_telegram`
- Проверка каналов, где бот является администратором
- Добавление отсутствующих каналов в БД
- Обновление метаданных существующих каналов

### 5.5. Управление инвайтами

**FR-5.1. Создание инвайта**
- Раздел "🎟 Ссылка для клиента"
- Выбор канала (последний созданный по умолчанию)
- Создание ссылки через `Bot.create_chat_invite_link`:
  - `creates_join_request=True`
  - `member_limit=1` (одноразовая) или `0` (неограниченная)
  - `expire_date` (опционально)
- Сохранение в `core.invites`

**FR-5.2. Обработка join-requests**
- Обработчик `ChatJoinRequest`
- Проверка наличия канала в БД
- Проверка лимитов инвайта:
  - `used_count < max_uses` (если `max_uses` задан)
  - `expires_at > now()` (если `expires_at` задан)
- Одобрение или отклонение запроса
- Инкремент `used_count` при одобрении
- Запись клиента в `core.clients`

**FR-5.3. Управление инвайтами (планируется)**
- Раздел "🔗 Мои ссылки"
- Список активных инвайтов
- Редактирование лимитов и TTL
- Отзыв инвайтов
- Статистика использования

### 5.6. Личный кабинет

**FR-6.1. Профиль подрядчика**
- Раздел "👤 Личный кабинет"
- Информация о подрядчике:
  - Telegram ID, username, full_name
  - Статус сессии (подключён/не подключён)
  - Текущий тарифный план
  - Лимиты подписки

**FR-6.2. Загрузка стандартной аватарки**
- Загрузка фото для использования при создании каналов
- Сохранение в профиле (планируется таблица `core.profile_avatars`)

**FR-6.3. Проверка сессии**
- Кнопка проверки статуса авторизации
- Переподключение при необходимости

### 5.7. Статистика и аналитика

**FR-7.1. Агрегированная статистика**
- Функция `aggregate_contractor_stats`:
  - Количество каналов
  - Количество файлов
  - Суммарные просмотры
  - Активные инвайты
  - Всего клиентов
  - Заблокированные клиенты
- Источник данных: `analytics.profile_overview` (view) или прямой подсчёт

**FR-7.2. Статистика просмотров**
- Периодическое обновление через `tasks.stats.refresh_views_periodic`
- Получение просмотров через `/rooms/get_views`
- Сохранение в `analytics.views_daily`
- Агрегация в `analytics.channel_stats`

**FR-7.3. События**
- Логирование событий в `analytics.events`:
  - `client_join` — клиент присоединился
  - `client_leave` — клиент покинул
  - `invite_used` — использован инвайт
  - `file_posted` — опубликован файл
  - `file_deleted` — удалён файл
  - `sync_run` — запущена синхронизация

---

## 6. Технические требования

### 6.1. Языки и версии

- **Python**: 3.11+
- **PostgreSQL**: 15+
- **Redis**: 7.2+
- **Docker**: 20.10+
- **Docker Compose**: 2.0+

### 6.2. Основные зависимости

**Bot:**
- aiogram 3.x
- asyncpg
- aioredis
- python-dotenv
- httpx

**Userbot:**
- FastAPI
- Telethon
- cryptography (Fernet)
- python-dotenv

**Worker:**
- Celery
- PyMuPDF (fitz)
- Pillow
- openpyxl
- redis
- asyncpg

**Backend:**
- FastAPI
- asyncpg
- python-dotenv

**Общие:**
- LibreOffice (бинарный)
- Ghostscript (бинарный, опционально)

### 6.3. Производительность

**Требования:**
- Обработка PDF до 50 страниц за раз
- Конвертация DOC/XLS до 100 страниц/листов
- Время рендеринга одной страницы: < 5 секунд
- Время публикации в канал: < 2 секунды
- Поддержка до 100 одновременных задач в очереди

**Оптимизации:**
- Параллельная обработка страниц (для многостраничных PDF)
- Кэширование превью в Redis
- Удаление исходных файлов после обработки
- TTL для временных данных

### 6.4. Надёжность

**Обработка ошибок:**
- Retry при ошибках Telegram API (403, 429, 400)
- Обработка FloodWait через `with_floodwait`
- Graceful degradation при недоступности сервисов
- Логирование всех ошибок без чувствительных данных

**Восстановление:**
- Автоматический перезапуск контейнеров
- Health-check для всех сервисов
- Проверка соединений с БД и Redis
- Валидация сессий перед использованием

### 6.5. Масштабируемость

**Горизонтальное масштабирование:**
- Несколько воркеров Celery (разные очереди)
- Возможность масштабирования бота (несколько инстансов)
- Репликация PostgreSQL (планируется)
- Redis Cluster (планируется)

**Вертикальное масштабирование:**
- Настройка `CELERY_CONCURRENCY` для воркеров
- Оптимизация запросов к БД
- Индексы в PostgreSQL
- Кэширование агрегированных данных

---

## 7. API спецификации

### 7.1. Userbot API

**Базовый URL:** `http://userbot:8001` (внутренний) или `https://orbitsend.ru` (внешний)

#### 7.1.1. Сессии

**GET /session/status**
- Проверка наличия и статуса сессии
- Параметры:
  - `contractor_id` (str, обязательный) — Telegram user ID подрядчика
  - `verify` (bool, опционально) — реконнект для проверки авторизации
- Ответ:
```json
{
  "has_session": true,
  "authorized": true
}
```

#### 7.1.2. Авторизация

**POST /login/phone/start**
- Начало телефонного логина
- Тело:
```json
{
  "contractor_id": "123456789",
  "phone": "+79991234567"
}
```
- Ответ:
```json
{
  "token": "random_token_here"
}
```

**POST /login/phone/confirm**
- Подтверждение кода
- Тело:
```json
{
  "token": "random_token_here",
  "code": "12345"
}
```
- Ответ:
```json
{
  "status": "ready",
  "me": {
    "id": 123456789,
    "username": "username",
    "phone": "+79991234567"
  }
}
```
или
```json
{
  "status": "2fa_required"
}
```

**POST /login/phone/2fa**
- Ввод пароля 2FA
- Тело:
```json
{
  "token": "random_token_here",
  "password": "password123"
}
```
- Ответ:
```json
{
  "status": "ready",
  "me": {...}
}
```

#### 7.1.3. Каналы

**POST /rooms/create**
- Создание приватного канала
- Тело:
```json
{
  "contractor_id": "123456789",
  "title": "Проект Иванова",
  "about": "Описание проекта"
}
```
- Ответ:
```json
{
  "channel_id": -1001234567890
}
```

**POST /rooms/add_bot_admin**
- Назначение бота администратором
- Тело:
```json
{
  "contractor_id": "123456789",
  "channel_id": -1001234567890,
  "bot_username": "@smetabot"
}
```
- Ответ:
```json
{
  "ok": true
}
```

**POST /rooms/set_photo**
- Установка фото канала
- Тело:
```json
{
  "contractor_id": "123456789",
  "channel_id": -1001234567890,
  "photo_b64": "base64_encoded_image_bytes"
}
```
- Ответ:
```json
{
  "ok": true
}
```

**POST /rooms/get_views**
- Получение просмотров сообщений
- Тело:
```json
{
  "contractor_id": "123456789",
  "channel_id": -1001234567890,
  "message_ids": [1, 2, 3],
  "limit": 100
}
```
- Ответ:
```json
{
  "ok": true,
  "views": {
    "1": 10,
    "2": 25,
    "3": 5
  }
}
```

**POST /rooms/get_admins**
- Список администраторов канала
- Тело:
```json
{
  "contractor_id": "123456789",
  "channel_id": -1001234567890,
  "limit": 50
}
```
- Ответ:
```json
{
  "ok": true,
  "admins": [
    {
      "id": 123456789,
      "username": "username",
      "full_name": "Full Name"
    }
  ]
}
```

### 7.2. Backend API (планируется)

**Базовый URL:** `http://backend:8000`

**GET /health**
- Health-check эндпоинт
- Ответ:
```json
{
  "ok": true
}
```

**Планируемые эндпоинты:**
- `GET /api/v1/contractors` — список подрядчиков
- `GET /api/v1/contractors/{id}` — информация о подрядчике
- `GET /api/v1/channels` — список каналов
- `GET /api/v1/channels/{id}` — информация о канале
- `POST /api/v1/invites` — создание инвайта
- `GET /api/v1/invites/{id}` — информация об инвайте
- `PUT /api/v1/invites/{id}` — обновление инвайта
- `DELETE /api/v1/invites/{id}` — удаление инвайта

---

## 8. Процессы и сценарии использования

### 8.1. Сценарий: Первый запуск подрядчика

1. Пользователь запускает бота (`/start`)
2. Бот проверяет наличие сессии через `/session/status`
3. Если сессии нет — предлагается WebApp для авторизации
4. Пользователь открывает WebApp, вводит телефон
5. Получает код, вводит код (и пароль 2FA при необходимости)
6. Сессия сохраняется в зашифрованном виде
7. Бот разблокирует основное меню

### 8.2. Сценарий: Создание канала и публикация документа

1. Подрядчик выбирает "🆕 Новый канал"
2. Вводит название канала
3. Загружает аватарку (опционально)
4. Бот вызывает `/rooms/create` → Userbot создаёт канал
5. Бот вызывает `/rooms/add_bot_admin` → бот становится администратором
6. Устанавливается фото канала (если загружено)
7. Создаётся одноразовая ссылка-приглашение
8. Метаданные сохраняются в БД
9. Подрядчик загружает PDF-файл
10. Генерируется превью, пользователь выбирает страницы
11. Задача ставится в очередь `pdf`
12. Worker конвертирует PDF → PNG 300 DPI
13. Накладывается водяной знак
14. Файл публикуется в канал с `protect_content=True`
15. Метаданные сохраняются в `core.publications`

### 8.3. Сценарий: Клиент получает доступ

1. Подрядчик создаёт ссылку-приглашение
2. Отправляет ссылку клиенту
3. Клиент переходит по ссылке
4. Отправляет join-request
5. Бот проверяет лимиты инвайта
6. Одобряет запрос (если лимиты не превышены)
7. Клиент получает доступ к каналу
8. Может просматривать опубликованные документы (без возможности форвардить)

### 8.4. Сценарий: Обновление статистики

1. Периодическая задача `refresh_views_periodic` (каждые 30 минут)
2. Для каждого канала вызывается `/rooms/get_views`
3. Полученные просмотры сохраняются в `analytics.views_daily`
4. Агрегируется статистика в `analytics.channel_stats`
5. Обновляется view `analytics.profile_overview`

---

## 9. База данных

### 9.1. Схемы

База данных разделена на логические схемы:

- **`core`** — основные сущности (подрядчики, каналы, публикации, инвайты, клиенты)
- **`billing`** — тарифы, подписки, лимиты, подарки
- **`analytics`** — статистика, события, представления
- **`referrals`** — реферальная программа
- **`admin`** — административные функции

### 9.2. Основные таблицы

#### 9.2.1. core.contractors
- `id` (BIGSERIAL PRIMARY KEY)
- `tg_user_id` (BIGINT UNIQUE) — Telegram user ID
- `username` (TEXT)
- `full_name` (TEXT)
- `status` (TEXT, CHECK: 'active'|'blocked')
- `created_at` (TIMESTAMPTZ)

#### 9.2.2. core.channels
- `id` (BIGSERIAL PRIMARY KEY)
- `contractor_id` (BIGINT FK → contractors.id)
- `tg_chat_id` (BIGINT UNIQUE) — Telegram chat_id канала
- `title` (TEXT)
- `username` (TEXT)
- `created_at` (TIMESTAMPTZ)
- `created_by_bot` (BOOLEAN)
- `synced` (BOOLEAN)
- `last_synced_at` (TIMESTAMPTZ)

#### 9.2.3. core.publications
- `id` (BIGSERIAL PRIMARY KEY)
- `channel_id` (BIGINT FK → channels.id)
- `message_id` (BIGINT) — Telegram message_id
- `file_name` (TEXT)
- `file_type` (TEXT)
- `views` (INT) — количество просмотров
- `posted_at` (TIMESTAMPTZ)
- `deleted` (BOOLEAN)
- UNIQUE (channel_id, message_id)

#### 9.2.4. core.invites
- `id` (BIGSERIAL PRIMARY KEY)
- `channel_id` (BIGINT FK → channels.id)
- `token` (TEXT UNIQUE) — ссылка-приглашение
- `expires_at` (TIMESTAMPTZ)
- `max_uses` (INT)
- `used_count` (INT)
- `editable` (BOOLEAN)
- `created_at` (TIMESTAMPTZ)

#### 9.2.5. core.clients
- `id` (BIGSERIAL PRIMARY KEY)
- `channel_id` (BIGINT FK → channels.id)
- `invite_id` (BIGINT FK → invites.id)
- `tg_user_id` (BIGINT)
- `username` (TEXT)
- `full_name` (TEXT)
- `joined_at` (TIMESTAMPTZ)
- `blocked` (BOOLEAN)

#### 9.2.6. billing.plans
- `id` (BIGSERIAL PRIMARY KEY)
- `code` (TEXT UNIQUE, CHECK: 'FREE'|'PRO'|'BUSINESS')
- `name` (TEXT)
- `price_month` (NUMERIC(10,2))
- `features` (JSONB) — возможности тарифа
- `channels_limit_one_off` (INT)

#### 9.2.7. billing.subscriptions
- `id` (BIGSERIAL PRIMARY KEY)
- `contractor_id` (BIGINT UNIQUE FK → contractors.id)
- `plan_id` (BIGINT FK → plans.id)
- `status` (TEXT, CHECK: 'active'|'trial'|'grace'|'expired'|'cancelled')
- `starts_at` (TIMESTAMPTZ)
- `expires_at` (TIMESTAMPTZ)
- `source` (TEXT, CHECK: 'paid'|'gift'|'trial'|'admin_grant')

#### 9.2.8. analytics.views_daily
- `publication_id` (BIGINT FK → publications.id)
- `collected_at` (DATE)
- `views` (INT)
- PRIMARY KEY (publication_id, collected_at)

#### 9.2.9. analytics.channel_stats
- `channel_id` (BIGINT PRIMARY KEY FK → channels.id)
- `files_count` (INT)
- `views_total` (INT)
- `clients_total` (INT)
- `blocked_total` (INT)
- `last_updated` (TIMESTAMPTZ)

### 9.3. Представления (Views)

#### 9.3.1. analytics.profile_overview
Агрегированная информация о профиле подрядчика:
- Текущий план
- Лимиты
- Статистика каналов
- Активные инвайты
- Очередь подарков

### 9.4. Индексы

- `idx_channels_contractor` на `core.channels(contractor_id)`
- `idx_publications_channel_posted` на `core.publications(channel_id, posted_at DESC)`
- `idx_invites_channel` на `core.invites(channel_id)`
- `idx_clients_channel` на `core.clients(channel_id)`
- `idx_clients_invite` на `core.clients(invite_id)`

### 9.5. Миграции

- `0001_init.sql` — создание всех схем и таблиц
- `0002_add_client_unique_constraint.sql` — дополнительные ограничения

---

## 10. Безопасность

### 10.1. Защита контента

**Telegram-каналы:**
- Все каналы создаются с `ToggleNoForwardsRequest` (запрет форвардинга)
- Все публикации отправляются с `protect_content=True`
- Файлы публикуются только как `document` (не фото/видео)

**Водяные знаки:**
- Все изображения публикуются с водяным знаком (если указан текст)
- Настройки водяного знака: opacity, step, angle, color, font
- Тайловое наложение для защиты от удаления

### 10.2. Управление сессиями

**Шифрование:**
- MTProto-сессии шифруются Fernet перед сохранением
- Ключ шифрования: `SESSION_SECRET` (из переменных окружения)
- Сессии хранятся в `SESSIONS_DIR` с расширением `.session.enc`

**Изоляция:**
- Каждому подрядчику соответствует отдельная сессия
- Сессии не пересекаются между подрядчиками
- Автоматическое закрытие клиентов после операций

### 10.3. Инвайты

**Ограничения:**
- Одноразовые ссылки: `member_limit=1`
- TTL: `expires_at` (опционально)
- Лимит использований: `max_uses` (опционально)
- Отслеживание использований: `used_count`

**Join-requests:**
- Все инвайты создаются с `creates_join_request=True`
- Требуется одобрение подрядчиком или ботом
- Автоматическая проверка лимитов перед одобрением

### 10.4. Хранение данных

**Временные файлы:**
- Исходные файлы хранятся в Redis с TTL
- После обработки файлы удаляются из Redis
- PNG с водяным знаком не хранятся долговременно

**Логирование:**
- Логи не содержат чувствительных данных
- Ошибки логируются без payload'ов
- Аудит действий планируется в `admin.admin_actions`

### 10.5. Переменные окружения

**Секреты:**
- `BOT_TOKEN` — токен Telegram бота
- `API_ID`, `API_HASH` — данные для MTProto
- `SESSION_SECRET` — ключ шифрования сессий
- `DATABASE_URL` — строка подключения к PostgreSQL
- `REDIS_URL` — строка подключения к Redis

**Конфигурация:**
- Все секреты настраиваются через `.env` файлы
- `.env` не коммитится в репозиторий
- Используется `.env.example` для документации

---

## 11. Инфраструктура и развёртывание

### 11.1. Docker Compose

**Сервисы:**
- `traefik` — reverse proxy с Let's Encrypt
- `db` — PostgreSQL 15
- `redis` — Redis 7.2
- `backend` — FastAPI backend
- `userbot` — Userbot API
- `bot` — Telegram bot
- `worker_pdf` — Celery worker для PDF
- `worker_publish` — Celery worker для публикации
- `worker_office` — Celery worker для Office документов
- `worker_preview` — Celery worker для превью
- `flower` — мониторинг Celery

### 11.2. Переменные окружения

**Общие:**
- `TZ` — часовой пояс (UTC по умолчанию)
- `ENV` / `APP_ENV` — окружение (dev/prod)

**Bot:**
- `BOT_TOKEN` — токен Telegram бота
- `BOT_USERNAME` — username бота
- `DATABASE_URL` — PostgreSQL URL
- `REDIS_URL` — Redis URL
- `USERBOT_URL` — URL Userbot API
- `WEBAPP_URL` — URL WebApp для авторизации
- `CELERY_*` — настройки Celery
- `REQUIRE_INIT_DATA` — требовать init data для WebApp
- `TG_PROXY_URL` — прокси для Telegram (опционально)

**Userbot:**
- `API_ID` — Telegram API ID
- `API_HASH` — Telegram API Hash
- `SESSION_SECRET` — ключ шифрования сессий
- `SESSIONS_DIR` — директория для сессий
- `BOT_USERNAME` — username бота
- `USERBOT_FLOODWAIT_FALLBACK` — таймаут FloodWait

**Worker:**
- `REDIS_URL` — Redis URL
- `CELERY_*` — настройки Celery
- `WATERMARK_*` — настройки водяного знака
- `SOURCE_BLOB_TTL` — TTL исходных файлов (секунды)
- `FULLRES_BLOB_TTL` — TTL полных PNG (секунды)
- `PREVIEW_BLOB_TTL` — TTL превью (секунды)
- `METRICS_PORT` — порт для Prometheus метрик
- `PUBLISH_STATS_INTERVAL` — интервал публикации статистики
- `PUBLISH_STATS_SAMPLE` — размер выборки для статистики

**Backend:**
- `WEBAPP_URL` — URL WebApp
- `BOT_TOKEN` — токен бота (для встраиваемого скрипта)

### 11.3. Volumes

- `pgdata` — данные PostgreSQL
- `userbot_sessions` — зашифрованные сессии
- `traefik_letsencrypt` — сертификаты Let's Encrypt

### 11.4. Сети

- `default` — внутренняя сеть для сервисов
- `traefik` — сеть для Traefik и внешних сервисов

### 11.5. Health Checks

Все сервисы имеют health-check:
- `db`: `pg_isready`
- `redis`: `redis-cli ping`
- `backend`: HTTP `GET /health`
- `userbot`: HTTP `GET /health`

### 11.6. Развёртывание

**Локальная разработка:**
```bash
docker-compose -f docker-compose.yml -f docker-compose.dev.yml up
```

**Production:**
```bash
docker-compose up -d
```

**Миграции:**
- Автоматически применяются при первом запуске через `docker-entrypoint-initdb.d`
- Ручные миграции через отдельный контейнер (планируется)

---

## 12. Мониторинг и метрики

### 12.1. Prometheus метрики

**Worker метрики:**
- `celery_task_total` — общее количество задач
- `celery_task_success` — успешные задачи
- `celery_task_failure` — неудачные задачи
- `celery_task_retry` — повторные попытки
- `publish_document_total` — публикации документов
- `publish_document_retry` — повторные публикации

**Порты:**
- `worker_pdf`: 9464
- `worker_publish`: 9465
- `worker_office`: 9466
- `worker_preview`: 9467

### 12.2. Логирование

**Уровни:**
- `INFO` — информационные сообщения
- `WARNING` — предупреждения
- `ERROR` — ошибки
- `DEBUG` — отладочная информация (только в dev)

**Структура:**
- Логи без чувствительных данных
- Timestamp, уровень, компонент, сообщение
- Stack trace для ошибок

### 12.3. Flower

**Мониторинг Celery:**
- URL: `http://localhost:5555`
- Аутентификация: `FLOWER_BASIC_AUTH` (опционально)
- Мониторинг задач, воркеров, очередей

### 12.4. Статистика

**Агрегированная статистика:**
- Периодическое обновление через Celery Beat
- Сохранение в `analytics.channel_stats`
- Обновление view `analytics.profile_overview`

**Периодические задачи:**
- `update_views_daily` — каждый час
- `update_channel_stats` — каждый час
- `refresh_views_periodic` — каждые 30 минут
- `apply_queued_gifts` — раз в день

---

## 13. Ограничения и известные проблемы

### 13.1. Текущие ограничения

1. **Backend API**: Минимальная реализация, REST-контракт не завершён
2. **Статистика просмотров**: Не полностью интегрирована с userbot
3. **Управление инвайтами**: Раздел "🔗 Мои ссылки" ожидает backend-эндпоинтов
4. **Квоты и тарифы**: Таблицы есть, но бизнес-логика не полностью реализована
5. **Реферальная программа**: Инфраструктура есть, но не интегрирована
6. **Массовое удаление**: Временные ключи удаляются по одному

### 13.2. Известные проблемы

1. **LibreOffice ошибки**: Требуется мониторинг ошибок конвертации в production
2. **Ghostscript**: Опциональная зависимость, может отсутствовать
3. **FloodWait**: Обрабатывается, но может замедлять операции
4. **Сессии**: При потере сессии требуется повторная авторизация
5. **Большие файлы**: PDF > 50 страниц могут обрабатываться долго

### 13.3. Технический долг

1. Рефакторинг старых таблиц (`rooms`, `documents`, `messages` — для обратной совместимости)
2. Унификация обработки ошибок
3. Улучшение тестового покрытия
4. Документация API (Swagger/OpenAPI)
5. Оптимизация запросов к БД

---

## 14. Планы развития

### 14.1. Краткосрочные планы (1-3 месяца)

1. **Завершение Backend API**
   - REST-контракт для CRUD операций
   - Интеграция с ботом
   - Документация API

2. **Улучшение статистики**
   - Реальное время обновления просмотров
   - Графики и аналитика
   - Экспорт данных

3. **Управление инвайтами**
   - Полноценный раздел "🔗 Мои ссылки"
   - Редактирование TTL и лимитов
   - История использований

4. **Система квот**
   - Полная реализация лимитов по тарифам
   - Уведомления о превышении лимитов
   - Автоматическое применение подарков

### 14.2. Среднесрочные планы (3-6 месяцев)

1. **Реферальная программа**
   - Полная интеграция с системой
   - Награды за приглашения
   - Отслеживание прогресса

2. **WebApp админка**
   - Панель управления для подрядчиков
   - Управление каналами через веб-интерфейс
   - Расширенная статистика

3. **Интеграция платёжной системы**
   - ЮKassa / Stripe
   - Автоматическое продление подписок
   - Управление платежами

4. **Мобильное приложение**
   - API для мобильных клиентов
   - Push-уведомления
   - Офлайн-режим

### 14.3. Долгосрочные планы (6+ месяцев)

1. **Масштабирование**
   - Репликация PostgreSQL
   - Redis Cluster
   - Горизонтальное масштабирование воркеров

2. **Расширенная аналитика**
   - ML-модели для прогнозирования
   - Рекомендации для подрядчиков
   - Бизнес-интеллект

3. **Интеграции**
   - CRM системы
   - Бухгалтерские системы
   - Другие мессенджеры (планируется)

4. **Безопасность**
   - Двухфакторная аутентификация для подрядчиков
   - Аудит всех действий
   - Шифрование данных в покое

---

## Приложения

### A. Глоссарий

- **Подрядчик (Contractor)** — пользователь, который создаёт каналы и публикует документы
- **Клиент (Client)** — пользователь, который получает доступ к каналу по инвайту
- **Канал (Channel)** — приватный Telegram-канал для проекта
- **Публикация (Publication)** — опубликованный файл в канале
- **Инвайт (Invite)** — ссылка-приглашение в канал
- **Сессия (Session)** — MTProto-сессия для работы с Telegram от имени подрядчика
- **Водяной знак (Watermark)** — наложенный текст на изображения для защиты
- **Рендеринг (Rendering)** — конвертация документа в изображение

### B. Ссылки на документацию

- [aiogram Documentation](https://docs.aiogram.dev/)
- [Telethon Documentation](https://docs.telethon.dev/)
- [Celery Documentation](https://docs.celeryq.dev/)
- [FastAPI Documentation](https://fastapi.tiangolo.com/)
- [PostgreSQL Documentation](https://www.postgresql.org/docs/)

### C. Контакты и поддержка

- Репозиторий: (указать URL)
- Документация: (указать URL)
- Поддержка: (указать контакты)

---

**Версия документа:** 1.0  
**Дата создания:** 2024  
**Последнее обновление:** 2024  
**Автор:** SmetaBot Development Team

