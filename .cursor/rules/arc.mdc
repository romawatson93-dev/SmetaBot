---
alwaysApply: true
---

# 🤖 SmetaBot — архитектура проекта

## 🎯 Назначение
SmetaBot — это система автоматизации защищённой выдачи смет подрядчиков клиентам через Telegram. Сервис создаёт приватные каналы с защитой контента, конвертирует документы в PNG 300 DPI с водяным знаком и отслеживает активность пользователей.

## 🏗️ Архитектура системы

### Основные компоненты

#### 1. Telegram Bot (`bot/`)
- **Технология**: aiogram 3.x
- **Назначение**: Основной интерфейс для подрядчиков
- **Функции**:
  - Стартовое меню и мастер создания каналов
  - Загрузка и обработка документов (PDF, DOC, XLS)
  - Управление каналами и ссылками-приглашениями
  - Личный кабинет и профиль пользователя
  - Интеграция с WebApp для авторизации

#### 2. Userbot API (`userbot/`)
- **Технология**: FastAPI + Telethon (MTProto)
- **Назначение**: Управление Telegram-каналами от имени подрядчиков
- **Функции**:
  - Создание приватных каналов с защитой контента
  - Авторизация через телефонный номер
  - WebApp-интерфейс для входа
  - Управление правами доступа и администраторами

#### 3. Celery Worker (`worker/`)
- **Технология**: Celery + Redis
- **Назначение**: Фоновая обработка документов
- **Функции**:
  - Конвертация PDF → PNG (PyMuPDF)
  - Конвертация DOC/DOCX → PNG (LibreOffice)
  - Конвертация XLS/XLSX → PNG (LibreOffice + PyMuPDF)
  - Наложение водяных знаков
  - Публикация в каналы через Bot API
  - Генерация превью документов

#### 4. Backend API (`backend/`)
- **Технология**: FastAPI
- **Назначение**: REST API для управления данными
- **Функции**:
  - Health-check эндпоинты
  - WebApp демо-страницы
  - Планируется: CRUD для проектов, каналов, документов

#### 5. Общие библиотеки (`common/`)
- **preview.py**: Генерация превью документов
- **watermark.py**: Управление водяными знаками

## 🔄 Ключевые процессы

### Онбординг подрядчика
1. Пользователь запускает бота (`/start`)
2. WebApp проверяет существующую сессию или запускает куар код flow
3. Создание MTProto-сессии для подрядчика
4. Разблокировка основного функционала

### Создание защищённого канала
1. Выбор названия и аватарки канала
2. Userbot создаёт канал с `ToggleNoForwardsRequest`
3. Бот назначается администратором
4. Создание одноразовой ссылки-приглашения
5. Сохранение метаданных в PostgreSQL

### Обработка документов
1. Загрузка файла в чат
2. Сохранение в Redis с TTL
3. Генерация превью для выбора страниц
4. Постановка в очередь Celery для рендера
5. Конвертация в PNG 300 DPI с водяным знаком
6. Публикация в канал как `document` с `protect_content=True`

## 🗃️ Хранилища данных

### PostgreSQL
- **projects**: Проекты подрядчиков
- **channels**: Метаданные каналов
- **channel_files**: Записи о публикациях
- **channel_file_views**: Статистика просмотров
- **project_invites**: Ссылки-приглашения
- **profiles**: Профили пользователей

### Redis
- **pdf:*, doc:*, xls:*, png:***: Исходные файлы
- **renderpng:***: Готовые PNG с водяными знаками
- **preview:***: Превью документов
- TTL управляется переменными окружения

### Диск
- **SESSIONS_DIR**: Зашифрованные Telethon-сессии
- Шифрование через Fernet с `SESSION_SECRET`

## 🔐 Безопасность

### Защита контента
- Все каналы создаются с `ToggleNoForwardsRequest`
- Публикации только как `document` с `protect_content=True`
- Одноразовые ссылки-приглашения с `member_limit=1`
- Водяные знаки на всех изображениях

### Управление сессиями
- Отдельная MTProto-сессия для каждого подрядчика
- Шифрование сессий через Fernet
- Автоматическое закрытие клиентов после операций

### Обработка ошибок
- Retry-механизмы для Telegram API
- Обработка FloodWait через `with_floodwait`
- Логирование без чувствительных данных

## ⚙️ Конфигурация

### Основные переменные окружения
- **BOT_TOKEN**: Токен Telegram бота
- **API_ID/API_HASH**: Данные для MTProto
- **DATABASE_URL**: PostgreSQL подключение
- **REDIS_URL**: Redis подключение
- **SESSION_SECRET**: Ключ шифрования сессий
- **WEBAPP_URL**: URL WebApp для авторизации

### Docker-конфигурация
- **docker-compose.yml**: Основная конфигурация
- **docker-compose.dev.yml**: Разработка
- **docker-compose.override.yml**: Переопределения

## 📊 Мониторинг

### Метрики
- Prometheus-метрики на порту `METRICS_PORT` (9464)
- Статистика публикаций и ретраев
- Агрегированная статистика по подрядчикам

### Логирование
- Структурированные логи без чувствительных данных
- Отслеживание ошибок Telegram API
- Мониторинг производительности рендера

## 🚧 Текущие ограничения

### Не реализовано
- Полный REST API в backend
- Статистика просмотров в реальном времени
- Управление ссылками-приглашениями
- Система квот и тарифов
- Массовое удаление временных файлов

### Планируется
- Интеграция с системой биллинга
- Расширенная аналитика
- API для мобильных приложений
- Система уведомлений

## 🔧 Разработка

### Структура проекта
```
SmetaBot/
├── bot/           # Telegram бот
├── userbot/       # MTProto API
├── worker/        # Celery воркеры
├── backend/       # REST API
├── common/        # Общие библиотеки
├── infra/         # Docker и миграции
└── sessions/      # MTProto сессии
```

### Запуск в разработке
```bash
# Основные сервисы
docker-compose -f docker-compose.yml -f docker-compose.dev.yml up

# Отдельные компоненты
make bot-dev      # Запуск бота
make userbot-dev  # Запуск userbot
make worker-dev   # Запуск воркера
```

### Тестирование
- Unit-тесты в `*/tests/`
- Интеграционные тесты через Docker
- Тестирование MTProto через `login_userbot.py`